<div class="container-fluid mt-4 ps-5 pe-5">
  <h1 class="fs-1 fw-bold mb-4">Artists for "<%= query %>"</h1>

  <div class="list-group">
    <% results["paginated_artists"].forEach(function(artist) { %>
    <a
      href="/artist/<%= artist.id %>"
      class="list-group-item list-group-item-action py-2 px-3 d-flex flex-column flex-md-row justify-content-between align-items-md-center border-0 border-bottom text-decoration-none"
    >
      <div class="text-truncate" style="max-width: 100%">
        <div class="fw-semibold text-dark" style="font-size: 0.95rem">
          <%= artist.name %>
        </div>
 
      </div>
    </a>
    <% }); %>
  </div>

  <% const totalPages = Math.ceil(results.total_count_artists / 6); %> <% if
  (totalPages > 1) { %>
  <nav class="d-flex justify-content-center mt-4">
    <ul class="pagination custom-pagination">
      <% for (let i = 1; i <= totalPages; i++) { %>
      <li class="page-item <%= i === Number(page) ? 'active' : '' %>">
        <a
          class="page-link"
          href="/search?query=<%= query %>&search_option=<%= search_option %>&page=<%= i %>"
        >
          <%= i %>
        </a>
      </li>
      <% } %>
    </ul>
  </nav>
  <% } %>
</div>
